# 📢 Montoya: O Motor de Engajamento (Ativação)

> **Responsável:** Montoya
> **Missão:** "Quebrar a apatia". Monitorar o caos legislativo, filtrar o que importa e transformar em conteúdo viral para trazer o usuário para a plataforma.

## 🎯 Objetivo do Módulo
O `montoya` não espera o usuário perguntar. Ele ativamente busca informações públicas, simplifica a linguagem (estilo "fofoca cívica") e prepara o terreno para o disparo de mensagens.

---

## 🛠 Estrutura do Projeto

### Coletores por Nível

- **`coletor_federal_camara.py`** - Coleta proposições da Câmara dos Deputados (API)
- **`coletor_federal_senado.py`** - Coleta proposições do Senado Federal (API)
- **`coletor_estadual.py`** - Coleta notícias sobre ALESP via Google Search
- **`coletor_municipal.py`** - Coleta notícias sobre câmaras municipais do Vale do Paraíba

### Scrapers

- **`scraper_noticias.py`** - Scraper genérico de notícias usando Google Custom Search API

### Orquestrador

- **`coletor_principal.py`** - Orquestra a coleta de todas as fontes em paralelo

---

## 🚀 Como Usar

### Instalação

```bash
pip install -r requirements.txt
```

### Configuração

1. Coloque suas credenciais do Google Custom Search API em `google-credentials.json`
2. Ajuste os parâmetros no `coletor_principal.py` (DIAS_ATRAS, LIMITE_POR_FONTE, etc.)

### Execução

```bash
python coletor_principal.py
```

Os resultados serão salvos em `resultados_coleta.json`.

---

## 📊 Estrutura de Dados

O coletor gera um JSON com a seguinte estrutura:

```json
{
  "resumo": {
    "data_coleta": "2025-11-22T...",
    "total_geral": 19,
    "total_por_fonte": {
      "federal_camara": 5,
      "federal_senado": 0,
      "estadual_alesp": 10,
      "municipal": 4
    }
  },
  "dados": {
    "federal_camara": [...],
    "federal_senado": [...],
    "estadual_alesp": [...],
    "municipal": [...]
  }
}
```

---

## 📝 Fontes de Dados

- **Federal (Câmara):** API Dados Abertos da Câmara dos Deputados
- **Federal (Senado):** API Dados Abertos do Senado Federal
- **Estadual (ALESP):** Google Search em sites confiáveis (G1, Folha, Estadão, etc.)
- **Municipal:** Google Search em sites confiáveis sobre câmaras municipais do Vale do Paraíba

